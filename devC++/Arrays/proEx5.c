// Write a program to enter "n" number of digits.Form a number using these digits.

#include <stdio.h>

int main()
{
	int nums, digit[0], num_of_digits, i;
	
	printf("\n Enter the number of digits : ");
	scanf("\n %d", &num_of_digits);
	
	
}
